#!/usr/bin/env bash
# camp-progress — deprecated wrapper, use: camp progress
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
CAMP_DIR="$(cd "$SCRIPT_DIR/../.." && pwd)"
python3 "$CAMP_DIR/toolkit/scripts/camp" progress "$@"
