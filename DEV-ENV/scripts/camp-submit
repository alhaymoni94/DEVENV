#!/usr/bin/env bash
# camp-submit — deprecated wrapper, use: camp submit <phase>
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
CAMP_DIR="$(cd "$SCRIPT_DIR/../.." && pwd)"
python3 "$CAMP_DIR/toolkit/scripts/camp" submit "$@"
